# 4.0.3 手动图层与复位修复

- 节气移除全部点几何，仅当前节气文字在固定轨道位置显示，其他标签隐藏。
- 星座/星宿标签使用独立层，不受行星标签总开关影响；手动选择为二选一，重复点选保留选中项，开启后镜头定位到所选星空。
- 手动切换与复位暂停讲解，防止课程动作立即覆盖手动视图。复位取消镜头过渡、清理拖动和惯性，立即回到俯视全景，保留时刻与天体播放状态。
- HTML 引用本地脚本携带版本参数，太阳系页面也检查 SW 更新，修复旧缓存脚本与新 HTML 混用；测试版本 4.0.3。
- 验证从默认关闭讲解/标签的页面手动选择、星空标签实际可见、切换二选一、重复点击、鼠标拖动缩放后复位、近景/课程中复位、当前节气唯一显示，以及旧 SW 缓存兼容。部署测试，不更新正式应用。

## 实施结果

- `v403-ui-test.cjs` 从默认关闭讲解和行星标签的页面开始，通过真实点击、鼠标拖动与滚轮验证手动流程，并验证课程手动接管。四个季节均只显示一个当前节气名称，节气点几何已移除。
- `v403-cache-test.cjs` 先安装旧 SW 并证明未加版本的脚本会命中旧缓存，再访问新页面；新脚本与复位功能正常，旧缓存兼容检查通过。
- 测试容器 `timeview-test-v403` / 镜像 `timeview:test-v4.0.3`，公网显示 v4.0.3，本地脚本 URL 携带 `?v=4.0.3`。正式应用保持 v3.63。
- 服务端课程与用户新课程文件哈希部署前后一致。源码和数据备份为 `before-v403-code.tar.gz` / `before-v403-data.tar.gz`，后者已下载到工作区 `.deploy-check/`。
