# 4.0.4 显示规则调整

- 节气层保留四个固定名称：春分、夏至、秋分、冬至，同时显示当前节气并高亮；继续不绘制节气点。
- X、Y 坐标轴仅标注 X、Y，移除春分和夏至字样。
- 星座、星宿均为可关闭的开关；开启其中之一关闭另一个，允许全部关闭。关闭时保持镜头位置，手动切换仍暂停讲解，避免课程覆盖。
- 测试桌面、手机的手动开关状态和实际可见标签，验证当前节气为二分二至或普通节气时的四/五个名称。测试版本更新为 4.0.4；部署保留维护内容，正式应用待验收。

## 实施记录

- `.deploy-check/v404-check.cjs` 验证开星座→开星宿→关闭星宿→开星座→关闭星座，以及夏至显示四个名称、清明显示四个常驻名称加当前名称；本地桌面和手机通过。
- 测试容器 `timeview-test-v404` / 镜像 `timeview:test-v4.0.4`，公网版本 4.0.4；正式仍 3.63。
- 服务器备份 `before-v404-code.tar.gz`、`before-v404-data.tar.gz`，数据备份已下载。课程文件和用户新课程哈希与部署前一致。
