# 4.0.2 章节画面与轨道展示修复

**目标：** 新章节立即定位，旧卡片不残留；星空球半径从 220 收近到 108（冥王星轨道 82）；地球卡片回到左上角；比邻星外轨道可观察；补 favicon。

1. `assistant.js` 保留每行 scene，章节入场统一清理。中途续播恢复最近画面指令；无指令的开场根据本章首个 scene 或日期段落定位。行星章开场总览、星空章星座、更远探索旅行者、生日章俯视与星宿。
2. 场景 reset 只重置视图和临时层，不改所选日期。课程图层切换清除旧卡及其它专题层；手动开启星座/星宿也清除不相关信息卡。
3. 卡片共享定位按 `TIMEVIEW` 分支：地球左上，太阳系紧靠讲解框左侧。手机保留可滚动高度。
4. 三体双星 10 秒一周、外轨道 45 秒一周，各自遵循开普勒方程；分别加速，显式标识，暂停保持不动。物理模型默认接口保留共用时间，教学渲染通过可选 outerYears 指定外轨道演示时间。
5. 创建 SVG/ICO 天体轨道图标，页面注入图标引用，Docker 打包；域名根目录提供 `/favicon.ico` 消除已报告 404。
6. 更新测试小版本 4.0.2。验证连续跨章、跳章、中途恢复、手动图层、比邻星可见位移及暂停、两端卡片、favicon 返回类型。备份后只替换测试容器，维护内容保持原文件。

## 同轮追加

- 地球轨道每 15° 一个固定节气点，共 24 点；只更新当前点的高亮，不再附着节气文字到地球上。固定名称径向外移，优先显示当前与二分二至，避免名字挤在一起，放近后显示更多名称。
- 图层面板增加“地轴”，记住用户选择；二分二至课程默认显示，但尊重用户手动关闭。
- 底栏新增“复位”，清理内容卡与拖动惯性，镜头回到太阳系俯视全景，不改日期与播放状态。
- 二分二至课程的代表日期采用当日 23:59，确保当日交节之后高亮与所讲节气一致。

## 验证与部署记录

- 本地自然跨章、逐章跳转和地球视图恢复通过；比邻星 3 秒移动约 44 屏幕像素，暂停位置不变。
- 公网桌面 1365 / 手机 390：24 个固定点半径与 15° 间隔、节气高亮变化、地轴选择及刷新后保持、复位俯视且日期/播放不变、卡片布局均通过。
- 底栏增高时讲解框自动避让；小比例总览只显示当前节气名称，24 个点完整保留，拉近后逐渐显示其它固定名称。
- `https://timeview.site/favicon.ico` 和 www 域名同路径均 200 `image/x-icon`。图标由自有 SVG 渲染为 ICO，安装于 nginx 站点根目录。
- 当前测试容器 `timeview-test-v402-r3` / 镜像 `timeview:test-v4.0.2-r3`，页面版本 4.0.2，SW 缓存版本 `4.0.2-r2`（同轮追加需求更新缓存）。正式应用仍 3.63。
- 测试课程维护文件及用户新课程哈希与部署前一致；备份 `before-v402-code.tar.gz`、`before-v402-data.tar.gz`、`before-v402-r2-code.tar.gz`，数据备份也下载到 `.deploy-check/before-v402-data.tar.gz`。
- 脚本 `.deploy-check/v402-test.cjs`、`v402-controls-test.cjs`、`v402-earth-cards.cjs` 的审计对象仅由浏览器测试注入，产品代码未暴露调试接口。
